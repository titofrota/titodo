<!DOCTYPE html>
<html lang="en">
  <head>
    <title>TiTodo</title>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= action_cable_meta_tag %>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="A production ready example Rails app that's using Docker and Docker Compose.">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

    <%# Generated with: https://realfavicongenerator.net/ %>
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="manifest" href="/site.webmanifest">
    <link rel="mask-icon" href="/safari-pinned-tab.svg" color="#61b9d2">
    <meta name="msapplication-TileColor" content="#da532c">
    <meta name="theme-color" content="#ffffff">
  </head>

  <body class="flex flex-col h-screen justify-between">
    <main class="bg-gray-200 min-h-screen flex items-center justify-center py-4">
      <div class="mx-auto w-3/5">
          <div class="text-center mb-8">
            <h1 class="text-6xl font-bold text-gray-800 mb-2">TiTodo</h1>

            <% if [root_path, todo_lists_path].include?(request.path) %>
              <p class="text-xl text-gray-600">What needs to be done?</p>
            <% end %>
          </div>
        <%= yield %>
      </div>
    </main>
  </body>
</html>
