<%= turbo_frame_tag dom_id(todo_item), data: { controller: "autofocus" } do %>
  <div class="flex items-center justify-between p-4 <%= todo_item.completed ? "bg-green-100" : "bg-gray-100" %> shadow-md rounded-lg mb-4">
    <%= form_with model: todo_item, url: todo_list_todo_item_path(todo_item.todo_list, todo_item), method: :patch, data: { turbo_frame: dom_id(todo_item) }, class: "flex-1 flex items-center space-x-2" do |f| %>
      <%= f.text_field :name, autofocus: true, class: "rounded bg-[rgba(255,255,255,0.5)] p-2 w-full text-lg font-semibold text-gray-900 cursor-text focus:outline-none focus:ring-0" %>
      <%= f.submit "💾", class: "text-blue-500 hover:text-blue-700 cursor-pointer" %>
    <% end %>
  </div>
<% end %>
