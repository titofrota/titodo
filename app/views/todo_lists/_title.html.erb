<%= turbo_frame_tag dom_id(todo_list, :name) do %>
  <div class="group flex items-center space-x-2">
    <h1 class="text-2xl font-bold text-gray-900">
      <%= todo_list.name %>
    </h1>

    <%= link_to "✏️", edit_todo_list_path(todo_list), data: { turbo_frame: dom_id(todo_list, :name) }, class: "hidden group-hover:inline text-gray-500 hover:text-gray-700 cursor-pointer" %>
  </div>
<% end %>
